import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    checkBox:{
        borderWidth:0,
        backgroundColor:"white",
        marginHorizontal:0,
        paddingHorizontal:0
    }
  })